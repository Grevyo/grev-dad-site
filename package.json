{
  "name": "my-cloudflare-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/worker/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.6.0"
  }
}
