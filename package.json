{
  "name": "grev-dad",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:production": "wrangler deploy --env production",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply grev-dad-dev --local",
    "db:migrate:preview": "wrangler d1 migrations apply grev-dad-preview --remote --env preview",
    "db:migrate:production": "wrangler d1 migrations apply grev-dad-production --remote --env production"
  },
  "devDependencies": {
    "typescript": "latest",
    "wrangler": "latest"
  }
}
